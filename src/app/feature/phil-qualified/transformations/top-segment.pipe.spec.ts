import { PqvTopSegmentPipe } from './top-segment.pipe';

describe('TopSegmentPipe', () => {
  it('create an instance', () => {
    const pipe = new PqvTopSegmentPipe();
    expect(pipe).toBeTruthy();
  });
});
